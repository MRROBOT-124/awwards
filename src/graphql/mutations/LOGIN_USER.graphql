mutation LoginUser($userDetails: UserDetails!) {
  loginUser(userDetails: $userDetails) {
    user {
      _id
      createdAt
      email
      updatedAt
      username
    }
    errors {
      field
      message
    }
  }
}